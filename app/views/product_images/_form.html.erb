<%= form_for(@product_image) do |f| %>
  <% if @product_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_image.errors.count, "error") %> prohibited this product_image from being saved:</h2>

      <ul>
      <% @product_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="field">
      <%= f.label :product_id %><br>
      <%= f.collection_select :product_id, Product.all, :id, :product_name, :required => true, :class => 'form-control' %>
    </div>
    <div class="field">
      <%= f.label :pic %><br>
      <%= f.file_field :pic, :class => 'form-control' %>
    </div>
    <!-- <div class="field">
      <%= f.label :version %><br>
      <%= f.number_field :version, :class => 'form-control' %>
    </div> -->
    <br>
    <div class="actions">
      <%= f.submit :class => 'btn btn-primary form-control' %>
    </div>
  </div>
<% end %>
